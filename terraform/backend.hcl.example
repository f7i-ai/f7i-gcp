# Copy to backend.hcl (gitignored). Match CI: dev bucket + project are separate from prod.
#   cp backend.hcl.example backend.hcl
#   terraform init -reconfigure -backend-config=backend.hcl
#
# Dev resources + state bucket project: anomaly-detection-dev-496103
bucket = "anomaly-detection-dev-496103-terraform-state"
prefix = "terraform/gcp/dev"
