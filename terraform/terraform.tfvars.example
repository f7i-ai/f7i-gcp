# Copy to terraform.tfvars for local runs. CI sets project + environment from the target branch.
# Use environment = "prod" when planning the main stack locally (match state prefix in backend.hcl).
project_id  = "anomaly-detection-496003"
region      = "australia-southeast1"
environment = "dev"
