# Copy to terraform.tfvars for local runs. CI uses GitHub variables (see terraform-main.yml).
# Dev stack targets the anomaly-detection-dev project; prod uses GCP_PROJECT_ID_PROD.
project_id    = "anomaly-detection-dev-496103"
region        = "australia-southeast1"
environment   = "dev"
